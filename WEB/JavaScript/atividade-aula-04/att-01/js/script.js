function contar() {
    contando = document.getElementById(iniciar)
    if (iniciar){
        for (cont=0; cont <= 100; cont++){
            console.log(cont)
        };
        
        for (cont=0; cont <= 100; cont += 2){
            console.log(cont)
        };
        
        for (cont=1; cont <= 50; cont += 3){
            console.log(cont)
        };
    };
};

